STATE:
user
  creatures
  inventory

MasterLists:
  creatures
  items
  recipes




LOCATION
  Area.jsx
    summonCreature
      state.user.creatures.push(randomCreature from state.creaturedById)

CREATURE HOME
CreatureHome.jsx
  fetchItem
    state.user.inventory.push(randomItem from clickedCreature.items)

CRAFTING
  craft
    if (craftingItems1 + craftingItems2) are not on recipeList, remove them
    if (craftingItems1 + craftingItems2) are on recipeList, remove them and userItems.push(newItem)
